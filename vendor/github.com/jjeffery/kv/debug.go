package kv

/*
import (
	"fmt"
)

var debug debugT = false

type debugT bool

func (d debugT) Printf(format string, v ...interface{}) {
	if d {
		s := fmt.Sprintf(format, v...)
		fmt.Println(s)
	}
}
*/
